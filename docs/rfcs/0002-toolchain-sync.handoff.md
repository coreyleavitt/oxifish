# RFC 0002 Toolchain sync — handoff

- **Stage:** 4 code-review — **COMPLETE (2026-07-10). Floor reached at round 4: 0 Critical / 0 High / 0 Medium; both standing reviewers (security, design) signed off clean.** 4 rounds total: r1 5 reviewers (0C/1H/6M/6L) → mandate fix-through-Medium → r2 found 1H/2M (guard self-test, notice gate, doctest guard) → fixed → r3 found 1M/4L (all fixed inline, incl. batched Lows) → r4 clean. Final gates: **530 passed** (CI's exact invocation), uv lock --check, ruff check/format, mypy --strict (19 files), release.yml YAML parses. Nothing committed.
- **Remaining Lows (deferred per mandate):** R8 (STREAM_MODES ×3 → conftest constant), R9 (publish-ok/release-fail → PyPI live w/o tag; re-run recoverable), R10 (`uv lock` full re-resolve on the [skip ci] commit), R12 (ref PATCH assumes main unprotected — currently true), R13 (informational). R7 wontfix. S1 residual cancellation window documented in-file, accepted.
- **COMMITTED & PUSHED (2026-07-11), CI FULLY GREEN** at 860d487 (all 16 jobs). 12 commits: 8 RFC 0002 slices/review + 4 post-push CI fixes. First-ever CI run (829d82c) surfaced: (1) stubtest flagged NamedTuple/StrEnum re-exports — fixed via module-qualified `enum.StrEnum`/`typing.NamedTuple` (the module is its own stub; local miss caused by venv mypy 2.2.0 vs locked 1.19.0 — venv now aligned, stubtest added to local gates, see memory `local-gates-must-mirror-ci`); (2) Windows sed variants mangle the translator test — `TestReleaseSemverTranslator` now skipif win32 (the chain only executes on Linux runners); (3) cargo-audit: pyo3 0.27.2 RUSTSEC-2026-0176/0177 — **pyo3 upgraded to 0.29.0** (zero source changes; cargo 60 + clippy + fmt green in Docker; 530 pytest + exact-string pins + stubtest green against rebuilt .so).
- **Resume:** MANDATORY TestPyPI `workflow_dispatch` dry-run (the only unproven parts: gh-api tree-commit mechanics, in-GHA lockfile refresh, wheel builds incl. the new setup-python 3.14 step) → then releases unblock; 0.2.0 release material sits in the 0001 handoff's Breaking Changes section.
- **Outstanding before any real release:** (1) push, then run the MANDATORY TestPyPI `workflow_dispatch` dry-run — the `gh api` tree-commit mechanics, in-GHA `uv lock`/`cargo update -p oxifish`, and wheel builds are the only parts not locally provable; (2) releases were blocked in the slice-3→6 window — slice 6 has landed, so the block lifts once the dry-run passes.
- **Round 2 verdict:** no round-1 reversal overturned (both re-verified adversarially with new evidence: pyo3-introspection structurally inapplicable — function-based pymodule + PyO3 0.27 pin; maturin has no git-tag versioning, so no third version option). One round-1 mechanism FALSIFIED and deepened (enum check), three structural blind spots closed.
- **Context:** the RFC draft PREDATES RFC 0001's landing and stage-4 review; round 1 doubles as the reconciliation pass. Corey chose to run `/architect` directly rather than a separate stage-1 revision (his call, 2026-07-10); the round's fixes should bring the RFC current AND sliced.

## Ground truth the draft is stale against (reviewers briefed on this)
- `python/oxifish/__init__.pyi` DELETED (RFC 0001 stage-4 review); fully-annotated `__init__.py` is the type source; hand-written `python/oxifish/_oxifish.pyi` types the compiled module, gated in CI by `mypy.stubtest` (typecheck job).
- `Mode`/`Padding` StrEnums validated at exactly one Rust site each (`parse_mode`/`parse_padding`, src/key.rs); pytest pins the value sets + case-sensitivity.
- Version still duplicated (Cargo.toml + pyproject.toml, both 0.2.0); release.yml still seds; no agreement check — item 1 stands.
- README rewritten for 0.2.0, examples manually verified but not executed in CI — README item stands.
- Old `pad`/`unpad`/`PaddingStyle` deleted — the draft's "four representations" framing and "426-line stub" facts are stale.
- Item 2/4 (pyo3-stub-gen) possibly moot or a regression vs the hand-written-stub + stubtest steady state — round 1 adjudicates.
- RFC 0001 work is UNCOMMITTED on main (0.2.0 not shipped); recommended-but-declined-for-now: commit 0001 before 0002 implementation starts.

## Slices (r3 — 6; slice 1 is the only Docker rebuild)
- [x] 1 [D] Enum single-representation + pins — DONE 2026-07-10. src/key.rs const tables (`ModeSelector::ALL`, `PADDING_TABLE`) + shared `parse_catalog<T>` helper; new tests/test_enum_sync.py (round-trip + anchored-parse + TestAnchoringMatters regression pin); lists introspected in test_one_shot_modes/test_streaming/test_hypothesis_chunking/test_ecb/test_one_shot_cbc. Fail-first proofs done (bogus member, doctored stale message). Docker gates green (fmt/clippy/cargo test 60); fresh .so installed. Host suite **486 passed** (474+12). Left untouched by design: IV_MODES, CBC_PADDINGS/ECB_PADDINGS (not in slice scope).
- [x] 2 [H] Executable docs — DONE 2026-07-10. README 7 fences + module docstring rewritten under the three rules; new tests/test_readme.py (fence-count assert + per-fence 0..i execution, failure names fence + fix action); ci.yml pytest line now `uv run pytest tests/ --doctest-modules python/oxifish -v`; rustup line in Development. Fail-first proof: typo'd fence #3 → tests [3]-[6] failed naming fence #3; reverted byte-identical. Host suite **495 passed** (486+8+1 doctest).
- [x] 3 [H] Toolchain gates — DONE 2026-07-10. ci.yml: cargo test/clippy --locked, uv sync --locked (test+typecheck), pytest line += --hypothesis-profile=ci, ruff/mypy scope += scripts/; benchmark.py 2 mypy fixes. Proofs: scratch-copy lockfile-drift fails uv lock --check; invalid profile name hard-errors (flag honored); YAML parses. Suite **495 passed** with CI's exact invocation. **RELEASES BLOCKED until slice 6.**
- [x] 4 [H] Dockerfile delete — DONE 2026-07-10 (inline). Grep re-confirmed zero references outside RFC docs; also removed the now-orphaned .dockerignore (same rationale — nothing builds images; the dev sandbox bind-mounts). README rustup line already landed in slice 2.
- [x] 5 [H] Version + floor checks — DONE 2026-07-10. New tests/test_toolchain_sync.py: pure `_assert_versions_equal` (three-way `Version()` equality, importlib.metadata/pyproject.toml/Cargo.toml, no custom translation) + pure anchored-regex parsers for the six floor surfaces (classifiers/mypy/ruff via tomllib, ci.yml matrix + release.yml `--interpreter` via anchored regex) + self-tests proving the two regex parsers raise loudly (naming file + expected shape) rather than silently no-op on a reformatted line. `TestReleaseInterpreterFloor` is `xfail(strict=True, reason="release.yml interpreter list is 3.10-3.13; fixed in RFC 0002 slice 6")` — the other five surfaces are hard passes today. `packaging>=25.0` added as explicit dev-group entry (alphabetical slot before pykeepass); `uv lock` + `uv lock --check` green. ci.yml typecheck job: `uv sync --locked --dev` → `--reinstall-package oxifish`, plus a new `uv run pytest tests/test_toolchain_sync.py` step placed immediately after the (now cache-fresh) install/build step, before mypy/stubtest, so a stale-metadata or floor-drift failure surfaces fast rather than after paying for the type-check steps. Fail-first proofs: scratch-dir desynced/malformed-version pairs (never the real files) fail with messages naming both scratch paths; doctored block-sequence/folded-scalar/empty-value YAML text raises `ValueError` naming the file; release.yml xfail confirmed xfailing (strict) today. YAML sanity parse (`uvx --with pyyaml`) green on both workflow files. Host suite **510 passed, 1 xfailed** (495 + 15 new + 1 xfail). Lint/type gates on touched files (ruff check, ruff format --check, mypy --strict) all green.
- [x] 6 [GHA] Release pipeline repair — DONE 2026-07-10 (local half; TestPyPI dry-run pending push). release.yml: SemVer translator at all 4 write sites (3 verbatim bash sed copies + mirrored PowerShell `-replace` chain with single-quoted backrefs); new scripts/check_release_version.py in-pipeline three-way assertion in prepare-version via `uv run --with packaging --no-project` (the [skip ci] fix; heredoc rejected — verified IndentationError); prepare-version refreshes uv.lock (`uv lock`) + Cargo.lock (`cargo update -p oxifish`, not --offline: no primed registry cache in that job) and commits both via the extended gh-api tree mechanics; `--target needs.prepare-version.outputs.commit_sha` on both gh release branches; interpreter list → 3.11–3.14. tests/test_toolchain_sync.py: xfail REMOVED (now hard pass) + TestReleaseSemverTranslator (sed chain extracted from release.yml, run via subprocess over all 6 input shapes, Version-equality round-trip) + 3-copies-byte-identical sync test + parser self-test. Scratch --project proof of uv version bump output shapes. Suite **521 passed** w/ CI's exact flags.
Order: 1, 2, 3, 4, 5, 6. Deps: 2 before 3 (same ci.yml line); 5 before 6; 3 blocks releases until 6.

## Open forks (awaiting Corey)
- None. Round-2 resolutions were evidence-backed recommendations (veto-able): the enum fix costs the "zero rebuilds" property (r2 claimed zero; r3 has exactly one Docker slice) in exchange for actually delivering the invariant.

## Key decisions (round 2, 2026-07-10)
- **Enum mechanism deepened (round-1 claim falsified by depth lens):** the error message's valid-set is a hand-typed literal in the catch-all — parsing it verified enum↔message, NOT enum↔arms. r3 fix: one const table becomes the parse table AND the message source (arms/message/valid-set cease to be separate things); small Rust refactor, byte-identical output proven by existing exact-string pins. Costs r2's "zero rebuilds" — accepted, it delivers the actual invariant.
- **`[skip ci]` blind spot (found independently by 2 lenses):** the bump commit never triggers ci.yml, so the version assertion must ALSO be a release.yml `prepare-version` step. Syntactically-bad SemVer fails safe (cargo hard-errors — reproduced); the release-side assertion covers valid-but-wrong.
- **Lockfile self-version desync:** `uv version --bump` rewrites uv.lock's own oxifish entry (probed); Cargo.lock same shape; release commits only manifests → slice 3's --locked gates would break main after first release. Fix: prepare-version refreshes+commits both lockfiles (slice 6); releases blocked in the 3→6 window.
- **Translation spec completed:** verified sed chain over the full uv-version output set incl. chained `aN.devM`; `pep440_of` doesn't exist — `packaging.version.Version` parses SemVer-hyphen forms directly (probed equal).
- **Six floor surfaces, not four** (+ mypy python_version, ruff target-version); checks live as pytest tests in tests/, not scripts/ (redundant matrix execution = stated trade-off).
- **Doc-rewrite rules hardened:** random-bytes ciphertext deterministically fails decrypt (probed); NONE needs block-aligned lengths (probed); per-fence parametrized harness for real attribution.
- **Misc adopted:** `gh release create --target` fix; `--reinstall-package oxifish` cache guard; `packaging` explicit dev dep; cargo-audit + SECURITY.md support-matrix → Non-Goals; snapshot-table sentence; failure-messages-must-name-source-and-fix rule; README rustup line; TestPyPI dry-run upgraded to MUST.

## Key decisions (round 1, 2026-07-10)
- **Stub generation (r1 items 2/4) DROPPED** — closed by RFC 0001: 4 symbols / 122-line hand stub / stubtest CI-gated and already caught real defects; pyo3-stub-gen cannot emit the load-bearing `@final`/`@disjoint_base` (workaround disables that stubtest check) and loses Literal precision. Revisit trigger: compiled surface grows to dozens of symbols.
- **Version single-sourcing REVERSED** — pyproject.toml stays truth. Hard evidence: `uv version` errors on `dynamic` version fields (probed live: "We cannot get or set dynamic project versions"); release.yml's whole prepare-version job is `uv version`-based; Cargo has no bump. Instead: normalized (packaging.version) three-way CI assertion + fix the 4 release.yml Cargo.toml writes that currently inject invalid SemVer (`0.2.1a1`, `0.2.0.dev47`) — a latent prerelease-path failure found this round.
- **Live bug absorbed into scope**: release.yml builds 3.10 wheels (below floor) and never 3.14 → change 2.
- **Enum-sync mechanism**: runtime-behavior pytest pair (round-trip every member; parse valid-set from live error string), replacing the r1 draft's Rust-source-scraping script. Real gap confirmed: test lists are hand-enumerated, drift passes CI today.
- **README/doctest**: `--doctest-glob` is a silent no-op on this README (zero `>>>` blocks); mechanism = self-contained blocks (rejected fixture-prelude as a new synced surface) + tests/test_readme.py exec harness + --doctest-modules for docstrings.
- **Dockerfile: delete** (unreferenced, strictly weaker than ci.yml, wrong Python base; keeping-verified costs a CI job for zero added coverage). Design lens's "one executable recipe" satisfied by ci.yml itself.
- **New Non-Goals section** so exclusions are explicit: pyo3-stub-gen, CHANGELOG authoring, MSRV, dev-sandbox Docker recipe, runtime changes.
- RFC restructured around the invariant "every repo self-claim is derived or CI-verified", with a claim/source/check audit table as the Problem section.

## Review ledger (stage 4, round 1 — 2026-07-10)
| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| R1 | H | Examples model IV reuse under one key: README fence 3 reuses fence 2's `iv` (CBC, different plaintext); `__init__.py` docstring reuses its `iv` twice more incl. a CFB encryptor (example 3) — the exact anti-pattern README's own warning forbids. Exec-only gates can't catch semantic misuse. Fix also caught a 2nd instance (padding fence reused modes fence's iv ×5) and streaming fence now models the `enc.iv` read-back contract. | fixed | tests/test_readme.py::test_fence_execution_never_reuses_iv (wraps _encrypt_raw/_encryptor_raw, records (id(key), iv) pairs); fail-first proven against old text (failed at fence 2) |
| R2 | M | release.yml:106 `uv run --with packaging --no-project` unpinned — fresh PyPI resolve inside contents:write job. | fixed | pinned `packaging==25.0` (uv.lock's exact version) + `--python 3.11` (closes Low R11 too) |
| R3 | M | release.yml: bump commit lands, downstream build/publish leg fails → main permanently bumped, no release/tag, no warning. | fixed | new `always()`-guarded `release-failure-notice` job: fires iff pypi path + prepare-version succeeded + any downstream not-success; ::error + step-summary with state + recovery paths; no auto-revert (races concurrent pushes — comment documents) |
| R4 | M | release.yml:305 CPython 3.14 on macos/windows hosted runners unconfirmed → possible build-wheels leg failure. | fixed | unconditional actions/setup-python@v5 (3.11–3.14) in build-wheels; desk-checked: no-op for containerized manylinux, authoritative for macos/windows discovery (maturin-action's own documented pattern). TestPyPI dry-run remains the end-to-end proof |
| R5 | M | test_enum_sync.py:93,104,125-127 bare asserts — violates RFC's own "failure message names both sources + fix action" mandate. | fixed | all three sites message src/key.rs tables ↔ __init__.py enums + fix action, matching test_toolchain_sync.py idiom; no assertion weakened |
| R6 | M | IV_MODES (test_auto_iv.py:33, test_streaming.py:35) and CBC_PADDINGS/ECB_PADDINGS (test_hypothesis_chunking.py:69-70) hand-enumerated. | fixed | IV_MODES→list(Mode), CBC/ECB_PADDINGS→list(Padding) (confirmed full sets; NONE safe — strategy block-aligns) with load-bearing comments; fail-first DRIFT_PROBE proof (192→220 params, loud failures); collect counts 192→192, no loss |
| R7 | M | release.yml 3 verbatim bash sed copies vs a composite action — documented RFC tradeoff, guarded by byte-identical sync test. | wontfix | RFC round-2 decision; revisit if a 4th site appears |
| R8 | L | STREAM_MODES byte-identical in 3 test files → could be one conftest constant. | open | quality reviewer |
| R9 | L | publish-pypi success + create-release failure → package live without tag/release; recoverable via re-run failed jobs. | open | desk-check |
| R10 | L | release.yml:115 `uv lock` is full re-resolution (unlike scoped `cargo update -p oxifish`) — can drift transitive deps on the [skip ci] commit CI never re-tests. | open | desk-check |
| R11 | L | release.yml:106 no `--python` pin; tomllib needs ≥3.11, works today via runner default only. | open | desk-check |
| R12 | L | ref PATCH (release.yml:158) silently depends on main being unprotected (verified: currently unprotected). | open | gh api check by reviewer |
| R13 | L | check_release_version.py doesn't validate Cargo-SemVer syntax — `cargo update -p oxifish` immediately after catches it. Informational. | open | correctness reviewer |

Round 2 (2026-07-10, security + design on the fix batch — all R1–R6 fixes confirmed holding; new findings):
| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| N1 | H | test_readme.py `_guard_iv_reuse` had no permanent self-check that the wrap intercepts calls — a reroute away from `_encrypt_raw`/`_encryptor_raw` (names kept) would silently disable the guard. | fixed | `seen` returned + non-empty asserts per fence run; `TestIvReuseGuardCatchesReuse` committed self-test; fail-first proven both layers (setattr disabled → 7 loud failures; assertion neutered → DID NOT RAISE) |
| N2 | M | release-failure-notice gated on `prepare-version.result == 'success'` — a failure/cancel after the mid-step ref PATCH suppressed the notice in exactly the case it exists for. | fixed | `pushed=true` output written after `commit_sha` (so pushed⇒commit_sha set), job gated on `outputs.pushed == 'true'` (+ target==pypi belt-and-suspenders); 4-scenario desk-check in-file; failed-job outputs behavior documented with hedged sourcing (only skipped jobs yield empty needs.outputs) |
| N3 | M | Module docstring had no mechanical IV-reuse guard — reintroducing the round-1 bug passed `--doctest-modules` silently. | fixed | `TestModuleDocstringNeverReusesIv`: DocTestFinder/DocTestRunner under the guard + seen-non-empty self-check; fail-first proven (doctored docstring: new test fails, --doctest-modules stays green); __init__.py restored byte-identical |
| N4 | L | release-failure-notice inherited unused `contents: write`. | fixed | job-level `permissions: contents: read` (bundled w/ N2) |
| N5 | L | README "own IV" fence read as recommending hand-generated IVs. | fixed | one "illustrative only — real IVs come from an existing source (e.g. KDBX header)" clause (bundled w/ N1/N3) |

Gates after round-2 fixes (control loop, authoritative): **530 passed** (528 + N1(b) self-test + N3 doctest guard), uv lock --check, ruff check/format, mypy --strict (19 files), both workflow YAMLs parse.

Round 3 (2026-07-10, security + design on round-2 files — all N1–N5 fixes confirmed holding; security empirically re-proved the docstring guard catches what --doctest-modules misses and that the seen-backstop is the load-bearing inertness check):
| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| D1 | M | N5's caveat landed as a 239-char trailing code comment inside the README fence — hurts the readability it was meant to help. | fixed (inline) | caveat moved to prose lead-in; in-fence comment terse |
| D2 | L | test_readme.py file docstring didn't mention the file now also guards __init__.py's docstring. | fixed (inline) | docstring addendum |
| D3 | L | Three near-duplicate `assert seen` self-checks drifted (only one carried the full fix action). | fixed (inline) | shared `_assert_guard_exercised(seen, context)` — fix-action clause structurally can't drift |
| D4 | L | `permissions: contents: read` on release-failure-notice; job needs zero perms. | fixed (inline) | `permissions: {}` + comment |
| S1 | L | Scenario-4 comment overclaimed: a cancellation in the 2-line PATCH→`pushed=true` window leaves main bumped with no notice. Not closable from inside the step. | fixed (inline) | documented as accepted residual gap in the comment |

Gates after round-3 inline fixes: **530 passed**, ruff/format/mypy clean, release.yml YAML parses, uv lock --check green.

Round 4 (2026-07-10, security + design on the round-3 inline delta): **both clean, zero new findings** — security verified no check was weakened by the `_assert_guard_exercised` consolidation (incl. the raise-after-first-populate ordering) and that `permissions: {}`/the residual-window comment match reality; design confirmed all four round-3 fixes meet the bar. **Review loop terminated at the floor.**

Verified correct (round 1): src/key.rs const-table refactor byte-identical error strings, RFC 0001 contracts (GIL-copy, DecryptionError channel, zeroization) untouched; SemVer sed chain + PowerShell mirror equivalent on all 6+ version shapes (shell-verified); gh-api tree-commit mechanics, outputs wiring, --target, [skip ci] semantics, permissions all desk-checked clean; introspected test lists match RFC spec with no coverage loss; all 7 README fences + doctests execute; six-surface floor agreement; Dockerfile deletion reference-free; both workflows parse.
