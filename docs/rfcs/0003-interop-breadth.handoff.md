# RFC 0003 Interop breadth — handoff

- **Stage:** 4 code-review COMPLETE incl. LOW-FIX EXTENSION (2026-07-22) — ZERO open findings at any severity. Corey mandated "take out the lows now"; all ten Lows (L1-L6, L8, L10-L12) fixed in two batches (Python/docs, then Rust + one Docker cycle). Round-4 re-review (security + design) surfaced H1 — L12 fix-incompleteness: `_resolve_ctr_width` re-compared the caller's ORIGINAL object (`== 128`) after validation, silently widening a lying in-catalog subclass to 128 (live PoC) — fixed RED-first in the control loop, closure re-verified by the finding's own reviewer (round 5: nothing above Low). Round-4/5 cosmetic Lows L13/L14/L15 also fixed. Final state: **689 pytest + 90 cargo tests, all gates green** (clippy -D warnings, fmt, ruff check+format, mypy --strict, stubtest, uv lock --check), fresh .so (cargo-verified in the Batch-B container; the two later Rust edits are doc-comment-only). Entire RFC 0003 diff still UNCOMMITTED.
- **Resume:** commit + release authorized 2026-07-23. Committed at base version 0.2.0 (slice 6's hand-bump to 0.3.0 reverted: RFC 0002's release pipeline performs the bump itself on dispatch — pyproject/Cargo.toml/both locks' oxifish entries/venv dist-info all restored, three-way version test green); release = workflow dispatch bump=minor, prerelease=no, target=pypi → 0.3.0. Only remaining item: D1 VeraCrypt fixture on a non-WSL2 Linux host (recipe: `tests/fixtures/README.md`).
- **L12 decision (this session):** canonicalize via `int.__index__(value)` after the isinstance guard in both validators; bool rendering in the ctr_width catalog message changes `True`→`1` (pin updated — interpolated rendering, not a catalog-template change). **L4 decision:** unify to TwofishKey's pre-existing angle style, keep bytes unit; RFC §2 literal example updated mechanically per the mandate.
- **Fixture blocker still open** (VeraCrypt golden-volume, slice 4 — see that slice's entry below): does not block review, needs a host with kernel Twofish support to close.
- All 6 slices implemented 2026-07-21 (version 0.3.0), entire diff UNCOMMITTED.

## Round 2 outcome (r2→r3 changes)
- **Exhaustion contract corrected** (depth, verified in ctr 0.10.1 source): counter is RELATIVE to session start — wraps through zero, stream ends after exactly 2^N − 1 blocks regardless of IV; matches PyCryptodome Counter (the motivating formats). Implementation pinned to `try_apply_keystream` (a "distance to 2^N" pre-check would wrongly reject legitimate wrapping streams). r2's "2^N − c₀" formula and "all-ones-IV latent bug" claims were wrong. Exhaustion tests use `StreamCipherSeek`, not near-boundary IVs (those test wrap/divergence).
- **CTS oracle conflict adjudicated**: IEEE P1619/D16 Annex B Vectors 15–19 ARE non-block-multiple (feasibility read the PDF; depth's "block-aligned only" premise false) — CTS has an authoritative oracle; added bidirectional xts-mode cross-validation (encrypt-ours/decrypt-theirs + reverse) against self-consistent tweak-swap bugs; OpenSSL leg not needed.
- **Golden-volume fixture completed**: scripted `veracrypt --text --non-interactive --create` (pinned version, --filesystem=none, ≤1 MiB); master-key path = test derives header key via hashlib.pbkdf2_hmac, XTS-decrypts header with oxifish, asserts "VERA" magic + CRC32 (independent ground truth), then decrypts a data sector. Fixture is block-aligned → CTS coverage comes from vectors/cross-validation.
- **Slice 1 additions** (feasibility, all verified): cipher needs non-default `stream-wrapper` feature; generic-array→hybrid-array Block<C> audit (~15 sites); zeroize claim corrected (feature moved one hop deeper, NOT removed); aes 0.9.1 exists for cipher 0.5; keep slice 1 unsplit. Slice 5 must make `Session::ingest`'s Stream branch fallible (shared path, all widths + OFB).
- **New "Documentation & release collateral" section**: SECURITY.md first-party-XTS disclosure (pitch change: this is our own ~100 lines of mode code) + XTS Known-Limitations bullet; README "which construct do I need?" pointer; version 0.3.0 + Supported Versions update.
- **Ergonomics**: `key_size` = TOTAL bytes 32/48/64 (matches TwofishKey + pyca; r2's per-half was a footgun); migration docs show session-reuse loop (ECB no-padding sessions flush eagerly) not just per-block one-liner; Usage examples added to §2/§3; 512-byte-data-unit-index warning (byte_offset // 512, not physical sector); equal-halves check scoped facade-only (IEEE Vector 1 has Key1==Key2, engine must accept); half-order citation (IEEE 1619 Key1‖Key2 = VeraCrypt ks/ks2 — verified); `tweak` name kept over `data_unit` (term of art); Literal-only ctr_width typing rationale pinned; "no tags" xts-mode claim dropped (it has tags).

## Scope (from the 2026-07-12 conversation)
Mission framing (memory: `oxifish-project-goal`): THE maintained Twofish binding for Python. Interop gaps: VeraCrypt/TrueCrypt≥5.0 + dm-crypt XTS (TwofishXTS), split nonce∥counter CTR (ctr_width 32/64/128 BE), migrants from the dead PyPI `twofish` package (docs-only migration path). Prerequisite: coordinated cipher 0.4→0.5 generation migration.

## Round 1 outcome (major r1→r2 changes — all four lenses reported; fixes applied)
- **VeraCrypt Twofish-XTS self-test vectors DO NOT EXIST** (Tests.c is XTS-AES only; Twofish gets 1 ECB vector). Testing rebuilt: IEEE 1619 XTS-AES vectors prove the generic XTS layer; VeraCrypt golden-volume fixture proves Twofish interop; xts-mode 0.6 as cargo dev-dep oracle.
- **pykeepass vendored XTS/CTR is dead code** (chaining classes referenced but never defined → NameError; only MODE_CBC works — verified in the pinned dev-dep). Replaced by in-suite pure-Python reference XTS/CTR over the ECB idiom.
- **Slice 1 rescoped**: five-crate coordinated bump (twofish 0.8.0, cipher 0.5.2, cbc 0.2.1, ctr 0.10.1, cfb-mode 0.9.1, ofb 0.7.1; MSRV 1.85/edition 2024; engine.rs trait-rename port; zeroize rewiring — ctr 0.10 dropped the feature; RFC 0001 pinned-API prose re-verify).
- **`encrypt_block`/`decrypt_block` DROPPED** (reversed r1): RFC 0001 deliberately deleted them; blessed idiom `key.ecb_encryptor(padding=Padding.NONE).finalize(block)` documented instead (slice 2 = docs-only).
- **`TwofishXTS(key)` single concatenated 32/48/64-byte key** (was `(key1, key2)`): matches VeraCrypt master-keydata/dm-crypt/LUKS/pyca convention; kills the swap-bug and unequal-length error class. `tweak` keyword-only; range `0 <= tweak < 2**128` validated in facade (ValueError, not OverflowError); data unit 16..2²⁰ blocks; tweak-is-a-position-not-a-nonce doc contract; repr/pickle/eq + GIL-soundness contracts restated for the new class; in-repo XTS primary (xts-mode fails maintenance bar → dev-dep oracle only); sibling module src/xts.rs, engine/session untouched.
- **CTR exhaustion contract**: keystream = 2^N − c₀ blocks; cataloged ValueError, never a panic (ctr_width=32 makes RustCrypto's apply_keystream panic reachable; width-128 pathological-IV latent behavior pinned too). Divergence tests via near-boundary IVs, not 64 GiB payloads. ECB factories reject ctr_width via natural TypeError (pinned). Slices 5+6 merged (one [D]).
- Problem survey: TrueCrypt scoped to ≥5.0 (LRW/CBC pre-XTS → non-goals); dm-crypt/LUKS added (cbc-essiv already servable; xts-plain64 served; xts-essiv non-goal); ctr_width justified honestly (no named corpus; PyCrypto Counter-convention homegrown formats).
- Exact error-catalog strings now pinned in the RFC (house voice).

## Slices (r3 — 6 slices, 4 [D] rebuilds; deps: 1 before 3/4/5; 3 before 4; 2 independent)
- [x] 1 [D] cipher 0.4→0.5 generation migration — DONE 2026-07-21. All 6 crates on pinned train; engine.rs: 2 trait-import renames + 8 `_mut`-suffix drops (cipher 0.5 change, not in RFC's rename list) + 12 hybrid-array Block-site fixes; rustc 1.97.0; cargo 60/60, pytest 530/530, all pins unchanged, all gates green; benchmark equal-or-better every row; fresh .so in place. UNCOMMITTED.
- [x] 2 Migration docs for dead `twofish` package — DONE 2026-07-21. README migration section + 1 executable fence (count 7→8, RED→GREEN), SECURITY.md ECB-bullet sentence, ecb_* docstring cross-refs; 532 pytest + all host gates green. "Which construct?" pointer deliberately left to slice 4. UNCOMMITTED.
- [x] 3 [D] XTS engine (in-repo src/xts.rs; IEEE Annex B vectors incl. 15–19 CTS + Vector 1 equal-keys; bidirectional xts-mode oracle) — DONE 2026-07-21. `XtsCipher<C>` generic over cipher-0.5 16-byte-block ciphers; `encrypt_data_unit`/`decrypt_data_unit(&self, data: &[u8], tweak: u128) -> Result<Vec<u8>, XtsError>`; LE tweak encode + GF(2^128) doubling + IEEE CTS (next-to-last/last tweak swap) all in-engine; length validation 16..=2^20 blocks via `XtsError::InvalidLength` (no reachable panics); Zeroizing tweaks + explicit CTS-scratch zeroize + `ZeroizeOnDrop` forwarding impl with static assert. 18 new cargo tests: IEEE vectors 1–4, 10, 15–19 cited by number (15–18 are the true 17/18/19/20-byte CTS vectors; 19 is 512-byte block-aligned in the standard itself, despite its Annex heading); bidirectional xts-mode 0.6 cross-validation over every CTS residue 0..16 (fixed-seed SplitMix64); length-preservation, boundary (incl. real 16 MiB max round-trip), tweak edges 0/1/2^64/2^128−1, equal-halves engine acceptance. Vector sources: IEEE P1619/D16 PDF Annex B (primary), cross-checked byte-for-byte vs cryptopp TestVectors/xts.txt; Vector 10 additionally matches VeraCrypt Tests.c. cargo 78/78, clippy+fmt clean (CI flags), host pytest 532/532, uv lock --check OK. aes 0.9.1 + xts-mode 0.6.0 dev-deps locked. engine.rs/session.rs untouched; `#[allow(dead_code)]` on `mod xts;` until slice 4 consumes it. UNCOMMITTED.
- [x] 4 [D] XTS Python surface (TwofishKey two-layer pattern; golden-volume fixture first; 3 SECURITY.md items) — DONE 2026-07-21. `TwofishXTS` in `src/xts_py.rs` (`#[pyclass(subclass)]`, mirrors `key.rs` exactly: constructor takes two already-split key halves, `_encrypt_raw`/`_decrypt_raw` take `tweak: u128` directly since pyo3 converts Python ints natively, copy-while-GIL-held + `Python::detach` around the transform, owned input/output buffers explicitly zeroized -- beyond `XtsCipher`'s own schedule/CTS-scratch zeroization -- since XTS has no `engine::Session` to hide them behind); registered in `lib.rs`, `#[allow(dead_code)]` on `mod xts;` removed. Python facade in `python/oxifish/__init__.py`: `TwofishXTS(_TwofishXTS)` splits/validates the concatenated key (32/48/64 bytes, equal-halves rejected), validates `tweak` range in the facade (ValueError, never OverflowError), `__repr__` = `TwofishXTS(key_size=NN)` (bytes, not bits -- differs from `TwofishKey`'s bit-count convention per RFC §2's literal example), `key_size`/pickle/copy/repr inherited/enforced the same way `TwofishKey` is. Stub + `__all__` updated; `python -m mypy.stubtest oxifish` green. `tests/test_xts.py` (88 tests): misuse-machine exact-string pins for all 4 catalog entries, tweak edges (0/2**128-1 accepted, -1/2**128 rejected) + IEEE-generality labeling (>2**64-1), non-buffer TypeErrors, pure-Python reference XTS (~130 lines incl. self-consistency check) built over `ecb_encryptor/decryptor(padding=Padding.NONE)` cross-validated via Hypothesis across all 16 CTS residues x random keys/tweaks/lengths (bidirectional: encrypt-vs-reference and decrypt-vs-reference), repr/pickle/copy contracts, GIL-soundness (large-encrypt-releases-GIL + bytearray-mutation-during-encrypt, mirroring RFC 0001's tests ported to one-shot-only XTS). README: "which construct do I need?" pointer after the intro, XTS section + fence (`_EXPECTED_FENCE_COUNT` 8->9), documented IV-reuse-guard exemption (structural, not a special-cased carve-out -- the guard only wraps `TwofishKey._encrypt_raw`/`_encryptor_raw`, which `TwofishXTS` never calls). SECURITY.md: 3 items added (TwofishXTS zeroization note; first-party-XTS disclosure -- own ~100-line implementation, not RustCrypto's, 4-way validated; Known Limitations XTS bullet). Fixed 2 pre-existing clippy `-D warnings` failures surfaced by a newer clippy in the Docker toolchain while gating this slice's build (unrelated to slice 4's own diff: `src/xts.rs`'s `let mut theirs` from slice 3, `src/engine.rs`'s `repeat().take()` predating this RFC) -- both one-line fixes, noted here since "ALL gates must pass" caught them. All gates green: cargo 78/78 + clippy + fmt; host pytest 622/622 incl. doctest-modules; `uv lock --check`; ruff check+format; mypy --strict; stubtest. Fresh `.so` in place (1000:1000). **BLOCKER (fixture only, does not block the slice):** VeraCrypt golden-volume fixture not committed -- see `tests/fixtures/README.md`'s new "`twofish_xts.vc`" section for the full diagnosis (root cause: this WSL2 host's kernel lacks `CONFIG_CRYPTO_TWOFISH`, so `dm-crypt`-based mounting is impossible regardless of Docker privilege; VeraCrypt CLI install and volume *creation* both verified working) and a ready-to-run recipe for a machine with kernel Twofish support. UNCOMMITTED.
- [x] 5 [D] CTR width engine+surface — DONE 2026-07-21. Engine: `Transform::Ctr` split into `Ctr32`/`Ctr64`/`Ctr128` (`ctr::Ctr32BE`/`Ctr64BE`/`Ctr128BE`, all `Kind::Stream`), new `engine::CtrWidth` enum, `make_ctr` parameterized (returns `Transform` — the three widths are distinct monomorphized types); **`Session::ingest`'s Stream branch fallible**: ALL stream transforms (3 CTR widths + OFB) moved `apply_keystream`→`try_apply_keystream`, `StreamCipherError`→new `EngineError::KeystreamExhausted { width }` rendering the cataloged string exactly (OFB arm documented-unreachable: its `remaining_blocks()` is hardcoded `None` in ofb 0.7.1 source); `close_out` now finalizes the session even when `ingest` itself errors (pre-existing gap the new failure mode exposed; pinned by `ctr_exhausted_close_out_still_finalizes_the_session`). No independent counter pre-check, per RFC — the crate's `remaining_blocks` accounting IS the bound (`cipher` 0.5.2 `check_remaining` verified: on Err, buffer + counter untouched). Exhaustion tests reach the boundary via `#[cfg(test)] Session::seek_ctr_blocks_for_test` wrapping `cipher::StreamCipherSeek::seek` (byte-position API; works fine on `StreamCipherCoreWrapper` — no pre-advanced-constructor fallback needed). 8 new cargo tests (86 total): wrap-through-zero vs hand-computed counter blocks at low-32-bits=2^32−2, high-96-bits-never-carry, 3-width divergence at block 1 (**all-0xFF IV — RED caught that a uniform non-FF IV is a trap: no carry crosses the width boundary so widths 32/64 coincide at block 1**), last-legit-block-succeeds, one-shot + mid-session exhaustion exact-string (never panic), width-128-unreachable-at-same-seek, width-128 byte-identity regression vs pre-slice CTR path. key.rs: 4 raw methods grew trailing `ctr_width: Option<u32>` (`None` = facade's default-128 collapse, documented both sides); single validation site `parse_ctr_width` called from EVERY `new_session` arm — invalid width checked before mode (rejected regardless of mode), non-None valid width on cbc/cfb/ofb rejected voice-parallel to `reject_stream_padding`; both cataloged strings exact. Facade: `ctr_width: Literal[32, 64, 128] = 128` kwonly on encrypt (BOTH overloads)/decrypt/encryptor/decryptor; `_validate_ctr_width`+`_resolve_ctr_width` (facade catalog check → huge ints raise the cataloged ValueError, never OverflowError); ECB factories untouched (natural TypeError pinned). Stub raw signatures updated; module docstring gained doctested nonce8+`to_bytes(8,"big")` ctr_width=64 example; README Modes section: ctr_width prose + new fence (`_EXPECTED_FENCE_COUNT` 9→10) + Errors bullet; `test_readme.py` `guarded_*_raw` arities updated (self-check green). pytest: new tests/test_ctr_width.py (23 tests: exact-string pins across all 4 entry points + per-mode rejects + explicit-128-on-CBC-is-no-op pin, ECB TypeError pins ×2, width-128 byte-identity, block-1 divergence, wrap via near-boundary IVs ×{32,64}, pure-Python reference split-counter CTR over the blessed ECB idiom + hypothesis cross-validation all 3 widths with counter biased ±4 around the wrap, 1 MiB ctr_width=32 flow) + 2 TestErrorCatalog rows in test_misuse_and_concurrency.py. Python-side exhaustion deliberately skipped (unreachable <64 GiB; counter is relative so no near-limit-IV shortcut exists — pinned at Rust level instead; noted in test_ctr_width.py docstring). All gates green: cargo 86/86 + clippy(-D warnings) + fmt --check; host pytest 649/649 incl. doctest-modules; uv lock --check; ruff check+format; mypy --strict; stubtest. Fresh `.so` (from the maturin wheel) in place, 1000:1000. UNCOMMITTED.
- [x] 6 Benchmark + docs sweep + release collateral (0.3.0) — DONE 2026-07-21. `scripts/benchmark.py`: `ctr_width` rows (128/64/32, one-shot enc+dec across the existing 1 KB/100 KB/10 MB `PAYLOADS`, `CTR_WIDTHS: tuple[Literal[32, 64, 128], ...]` typed to satisfy `mypy --strict` overload resolution); `TwofishXTS` rows (512-byte data-unit loop, ~100 KB/rep via 200 independent one-shot calls each with its own tweak — the realistic disk-encryption access pattern, not one big call — plus a single 10 MB data unit exercising the block-aligned path near the throughput-relevant end of the 16 MiB ceiling); new-API only (neither construct exists on the old-API baseline); no threshold gate per RFC. Docs sweep: module docstring already introduces both `TwofishXTS` and `ctr_width` with doctested examples (slices 4/5, verified passing under `--doctest-modules`); README "which construct do I need?" pointer present and accurate (both anchors resolve); migration section <-> ECB <-> XTS <-> ctr_width cross-references coherent. One surgical fix: README "Errors" section's `ValueError` bullet enumerated the `ctr_width` causes (slice 5) but none of the XTS catalog entries (slice 4 gap) — added the four `TwofishXTS` causes (key length, equal halves, data-unit range, tweak range); `test_readme.py` 23/23 still green. Version -> 0.3.0: `pyproject.toml` (source of truth per RFC 0002) + `Cargo.toml` (hand-synced) + single-line version entries in `Cargo.lock`/`uv.lock`'s own `oxifish` self-entries (hand-edited, safe per RFC 0002's characterization of these as a single version-only line) + the host venv's hand-written dist-info directory (`oxifish-0.2.0.dist-info` -> `oxifish-0.3.0.dist-info`, `METADATA`'s `Version:` line) since `importlib.metadata.version("oxifish")` reads that, not a rebuilt wheel — no Docker rebuild needed, the `.so` embeds no version. `uv lock --check` passes without regen (`uv lock` was not needed). SECURITY.md Supported Versions: 0.3.x supported, 0.2.x and 0.1.x both EOL (existing convention: prior version drops to EOL on the next minor). RFC status header -> `implemented (0.3.0) — 2026-07-21`. All host gates green: pytest 649/649 + doctest-modules (incl. `tests/test_toolchain_sync.py`'s three-way version-agreement test, which is the one test that asserts the version and now passes at 0.3.0), `uv lock --check`, ruff check + format --check, `mypy --strict` (new `Literal[32, 64, 128]`-typed closures in benchmark.py required to satisfy strict overload resolution), stubtest, both workflow YAMLs parse. Benchmark run twice on this host (once contended with the gate suite, once quiet — consistent): every row 1.1–1.5 MB/s, i.e. the RustCrypto twofish crate's portable-impl block ceiling dominates; XTS rows (1.3–1.5) and all three ctr_width rows (1.1–1.5) sit on par with the CBC rows from the same run — no mode-level overhead visible, no regression signal (slice 1 recorded no numeric baseline, only "equal-or-better every row"; the RFC-0001-era ~2.5 MB/s figure is not reproducible on today's host state in ANY row incl. pre-existing CBC ones — host variance, not an RFC 0003 effect, since new and old rows moved together). Full table in the slice-6 completion report. UNCOMMITTED.

## Open forks (awaiting Corey)
- None. Round-1 reversals flagged for veto in the round-1 report (raw-block API dropped; single-key XTS constructor; in-repo XTS; ctr_width kept under honest justification); round 2 added the first-party-XTS SECURITY.md disclosure — same veto offer applies.

## Key decisions (this session)
- Feature RFC before abi3 RFC; hardening chores need no RFC.
- Layered correctness argument replaces the two false r1 testing pillars (official-vector-pinned block primitive → two independent mode-layer reimplementations → golden-volume interop fixture).

## Environment (unchanged from RFC 0001/0002 — critical)
- Cargo/maturin ONLY in throwaway `opensuse/tumbleweed` Docker (bind-mount project + ~/.cargo + ~/.rustup; zypper gcc python311 python311-devel python311-pip; `pip3.11 install --break-system-packages maturin`; PYO3_PYTHON=/usr/bin/python3.11; chown back 1000:1000). Scripts from the pyo3-0.29 upgrade are the working template. Slice 1 must check rustc ≥ 1.85 in the container.
- Host tests ONLY `uv run --no-sync pytest ...`; `uv sync`/plain `uv run` destroy the hand-wired 3.11 venv; `uv pip install --python .venv` is safe for tool alignment.
- Local gate set MUST mirror CI (memory: `local-gates-must-mirror-ci`): pytest w/ CI's exact flags, uv lock --check, ruff check + format --check, mypy --strict (venv mypy pinned to uv.lock's 1.19.0), **stubtest**, YAML-parse both workflows.
- Subagent briefs must include the Docker log-poll-don't-park instruction (memory: `subagent-docker-stall-pattern`).

## Review ledger (stage 4, round 1 — verified 2026-07-21)
| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| M1 | M | `_validate_ctr_width` lacks int type-guard: float `32.0` passes the `in` check and reaches PyO3's `Option<u32>` → generic uncataloged TypeError; str `"64"` renders a misleading catalog message ("invalid ctr_width 64") | fixed | isinstance guard → `TypeError: "ctr_width must be an int, got {type}"` (bool falls through to catalog ValueError by design); pinned by `test_ctr_width.py::TestCtrWidthTypeGuard` (RED-first, all 4 entry points) + TestErrorCatalog row; RFC Contracts §3 gained the one-line TypeError clarification |
| M2 | M | `KeystreamExhausted` raised via `update()` leaves session in Streaming state; a subsequent `finalize()` (0 further blocks) succeeds cleanly, silently masking the truncated stream | fixed | exhaustion now finalizes at the single `ingest` site (match replaces map_err); subsequent use raises cataloged "session is already finalized"; RED-first pins `ctr_width_32_direct_ingest_exhaustion_finalizes_the_session` + `..._close_out_after_direct_ingest_exhaustion_is_rejected_not_silently_successful` (cargo 88/88); engine.rs docs updated |
| M3 | M | `encrypt_block_raw`/`decrypt_block_raw` drop an un-zeroized stack copy of every XEX intermediate (`P_j⊕T_j` pre-encryption), contradicting the module doc's zeroization claim; engine.rs zeroizes the analogous CFB intermediate | fixed | `b.as_mut_slice().zeroize()` after copy-out in both helpers; doc comments state the discipline (hygiene fix — no observable behavior, existing suite green) |
| M4 | M | `Session::ingest` Stream branch drops the chunk's plaintext copy (`out`) unscrubbed when `try_apply_keystream` errors, unlike `padding_block`'s zeroize-before-Err | fixed | `out.zeroize()` in the Err arm of the same M2 restructure, before state transition + error return |
| M5 | M | XTS exact-16-MiB (2^20-block) accept boundary never exercised through the public Python API (MIN-accept and MAX+1-reject are; MAX-accept is Rust-only) | fixed | `test_xts.py::TestDataUnitLengthValidation::test_accepts_the_maximum_boundary` (16777216-byte round-trip, ~18 s, the module's only large-payload test) |
| L1 | L | `xts_py.rs` copies the full input (`to_vec`) before the 16 MiB bound check runs in the engine — oversized input pays the copy before rejection | fixed | `xts::validate_len` now `pub(crate)`, called on the borrowed slice before `to_vec` in both raw paths via the same `xts_err_to_py` mapping (byte-identical ValueError); engine check retained (defense in depth); existing exact-string reject pins green over the new path |
| L2 | L | OFB `unreachable!()` in exhaustion arm depends on `ofb` 0.7.x caret-range behavior (`remaining_blocks()` hardcoded None) — a patch bump could make it live | fixed | tripwire `engine.rs::ofb_core_remaining_blocks_is_hardcoded_none_the_unreachable_arms_load_bearing_assumption` asserts `remaining_blocks() == None` on the engine's own concrete OFB type; failing after a dep bump mandates converting the arm to a cataloged error |
| L3 | L | SECURITY.md says the Python-side key-bytes caveat applies "identically" to TwofishXTS, but `_split_xts_key` leaves two extra un-zeroized `bytes` halves | fixed | wording corrected 2026-07-22: caveat applies AND the split produces two additional un-zeroizable `key1`/`key2` bytes halves |
| L4 | L | repr style divergence: `<TwofishKey key_size=256>` (bits, angle) vs `TwofishXTS(key_size=64)` (bytes, parens) — unify bracket style while unshipped | fixed | TwofishXTS repr now `<TwofishXTS key_size=NN>` (bytes unit kept); RED-first pytest pin, RFC §2 literal example synced, repo-wide sweep found no other occurrence |
| L5 | L | no `__eq__`/`__hash__`-is-identity test for TwofishXTS (or TwofishKey — pre-existing systemic gap) | fixed | `test_misuse_and_concurrency.py::TestKeyIdentityEqualityContract` + `test_xts.py::TestIdentityEqualityContract` (identity semantics: same-key instances NOT equal — equality would require comparing secrets) |
| L6 | L | no structural pin that TwofishXTS lacks `encryptor`/`decryptor` (one-shot-only contract unpinned) | fixed | `test_xts.py::TestOneShotOnlySurface` (class + instance, 4 factory names) |
| L7 | L | invalid ctr_width tested only at 7/99 — no near-boundary (0, 16, 33, 127) or huge-int pins | fixed | bundled with M1 (same file): `TestMisuseMachine::test_invalid_ctr_width_one_shot_encrypt` parametrized over 0/7/16/33/127/2**64 |
| L8 | L | dual-violation ordering (invalid ctr_width + non-CTR mode → width error wins) implemented in `parse_ctr_width` but unpinned | fixed | pure core `ctr_width_for_mode` extracted (PyErr messages unassertable in cargo tests; strings/ordering byte-identical, mirrors the XtsError boundary-mapping pattern); pinned by `key.rs::ctr_width_invalid_value_beats_non_ctr_mode_mismatch_in_error_precedence` |
| M6 | M | (round 2, design) M1's TypeError split not extended to sibling `_validate_tweak`: `tweak="64"` still folds into the cataloged ValueError, rendering as an out-of-range rejection — module now has three type-error voices | fixed | non-int tweak → `TypeError: "tweak must be an int, got {type}"` (bool passes as int by design, pinned); range ValueError byte-identical; RED-first `test_xts.py::TestTweakTypeGuard` (4 tests, replaces old ValueError pin) + `TestErrorCatalog::test_non_int_tweak`; RFC §2 gained the parallel one-line clarification; 669 pytest + all gates green |
| L9 | L | (round 2, correctness+security) type-error-beats-mode/width-semantics precedence (e.g. `ctr_width="64"` + `mode=CBC` → TypeError wins) sensible but unpinned | fixed | `TestCtrWidthTypeGuard::test_type_guard_precedes_the_non_ctr_mode_check` (green immediately — characterization pin) |
| D1 | M | VeraCrypt golden-volume fixture absent — the RFC's primary Twofish-XTS interop proof | deferred | environment-blocked (WSL2 kernel lacks CONFIG_CRYPTO_TWOFISH); recipe ready in `tests/fixtures/README.md`; already tracked above |
| R1 | — | design-agent claim that `_validate_tweak` lacks a type check | refuted | `isinstance(tweak, int)` present at `__init__.py:597` and tested; only the `{tweak}` str-rendering nit survives (folded into M1's fix discussion) |
| F1 | — | follow-up idea, not a defect: `encrypt_sectors`-style bulk helper on TwofishXTS | noted | design agent; candidate future RFC material, not fix-loop scope |
| L13 | L | (round 4, design) repr bits-vs-bytes asymmetry (`TwofishKey`=bits, `TwofishXTS`=bytes) documented internally + in RFC but invisible to Python users (no class/`__repr__` docstring or README mention) | fixed | sentence added to `TwofishXTS` class docstring (the diverging sibling), stating bytes unit, the literal repr form, property agreement, and the contrast with TwofishKey's bits |
| L14 | L | (round 4, design) `ctr_width_for_mode` doc comment overclaims parity with the typed `XtsError` boundary pattern (returns `Result<_, String>`, not a typed error — fine for 2 messages/1 consumer, wording overstates) | fixed | reworded to "similar in spirit... though as a bare `String` rather than a typed error (two messages, one consumer)"; doc-comment-only Rust edit — installed .so unaffected, CI rebuild will re-verify fmt |
| L15 | L | (round 5, design) `TestLyingIntSubclassCanonicalization` class docstring narrated only the L12 vector; the H1 test pins a distinct downstream fix and lacked the suite's finding-ID tag | fixed | class docstring gained the second-half paragraph (H1: consume the canonicalized return, never re-compare the original); test comment tagged "Code-review finding H1 (round 4, L12 fix-incompleteness)" |
| L10 | L | (round 3, design) TestErrorCatalog map asymmetric: ctr_width has ValueError+TypeError rows, tweak only the TypeError row (range ValueError covered in test_xts.py instead) | fixed | `TestErrorCatalog::test_invalid_tweak` row added, map symmetric |
| L11 | L | (round 3, design) `_validate_ctr_width` docstring cross-references `_validate_tweak` but not vice versa | fixed | reciprocal cross-ref sentence added to `_validate_tweak` docstring |
| L12 | L | (round 3, security) `isinstance` admits int subclasses that could lie in comparisons vs their C-level PyLong payload | fixed | NOT theoretical after all — RED proved a lying ctr_width subclass silently collapsed onto the 128-bit default (no error), lying tweak leaked uncataloged OverflowError. Both validators now canonicalize via `int.__index__(value)` post-guard; pinned by `TestLyingIntSubclassCanonicalization` in test_ctr_width.py + test_xts.py; bool ctr_width catalog rendering changed `True`→`1` (interpolation, not template) |
| H1 | H | (round 4, security — L12 fix-incompleteness) `_resolve_ctr_width` discarded the validator's canonicalized return and re-compared the ORIGINAL object (`ctr_width == 128`): a lying-`__eq__` subclass with in-catalog payload 32 passed validation then silently widened to 128 (live PoC: ciphertext byte-identical to width 128, no exception) — all 4 entry points affected | fixed | control loop, RED-first: `resolved = _validate_ctr_width(ctr_width); return None if resolved == 128 else resolved` + docstring; pinned by `test_lying_in_catalog_ctr_width_still_encrypts_at_its_true_width` (all-0xFF-IV divergence proves width identity); caller audit: tweak path already consumed the return inline; 689 pytest + all host gates green (Python-only, no rebuild); closure re-verified by the finding's own reviewer |
